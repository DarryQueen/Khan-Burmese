$('#video-list').html("<%= escape_javascript(render('video_list')) %>");

// Disable form submission advances:
$('#video-search').bind("keypress", function(e) {
  if (e.keyCode == 13) {
    return false;
  }
});
$('#search').bind("blur", function(e) {
  this.focus();
});
