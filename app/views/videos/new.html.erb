<div class="row centered-form">
  <div class="col-md-6 col-md-offset-3">
    <div class="panel panel-default module">
      <div class="panel-heading">
        New Video
      </div>
      <div class="panel-body">
        <%= form_for @video, :html => { :class => 'form-update' } do |f| %>
          <div class="form-group">
            <%= f.text_field :title, :autofocus => true, :class => 'form-control', :placeholder => 'Title' %>
          </div>

          <div class="form-group">
            <%= f.text_field :youtube_id, :class => 'form-control', :placeholder => 'Youtube ID' %>
          </div>

          <div class="form-group">
            <%= text_field_tag 'video[subject]', nil, :class => 'form-control', :placeholder => 'Subject' %>
          </div>

          <div class="form-group">
            <%= f.text_area :description, :class => 'form-control', :placeholder => 'Description' %>
          </div>

          <%= f.submit 'Create', :class => 'btn btn-info center-block' %>
        <% end %>
      </div>
    </div>
  </div>
</div>


