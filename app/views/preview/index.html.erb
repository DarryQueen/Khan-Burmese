<div class="col-sm-12">
  <div class="panel panel-default module">
    <div class="panel-heading">
      <b>Previews</b>
    </div>
    <div class="panel-paragraphs">
      <p>Here you can find the list of prototypes. Note that none of these pages will work as intended; they are mockups for look-and-feel.</p>
    </div>
    <ul class="list-group">
      <% @pages.each do |page| %>
        <li class="list-group-item">
          <b><%= link_to page.titleize, params.merge({ :page => page }) %></b>
        </li>
      <% end %>
    </ul>
  </div>
</div>
